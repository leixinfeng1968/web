# Git自动推送功能使用指南

## 🎯 功能概述
现在可以自动将上传的课件文件推送到Git仓库（如GitHub、GitLab等），实现：
- ✅ 本地存储到uploads目录
- ✅ 自动推送到Git仓库
- ✅ 实时Git状态监控
- ✅ 完整的版本控制

## 🔧 准备工作

### 1. **创建GitHub仓库**
在GitHub上创建一个新的仓库：
```
仓库名称: 初中数学课件库
描述: 初中数学动态交互课件集合
公开/私有: 根据需要选择
```

### 2. **配置Git仓库**
在当前项目目录中执行以下命令：

```bash
# 初始化Git仓库
git init

# 添加远程仓库（替换为您的实际仓库地址）
git remote add origin https://github.com/您的用户名/初中数学课件库.git

# 设置默认分支为main
git branch -M main

# 创建uploads目录（如果不存在）
mkdir uploads

# 添加所有文件
git add .

# 初始提交
git commit -m "初始提交：创建课件上传系统"

# 推送到GitHub
git push -u origin main
```

### 3. **Git认证配置**
配置Git用户信息：
```bash
git config --global user.name "您的姓名"
git config --global user.email "您的邮箱"
```

## 🚀 使用方法

### 启动智能上传服务器
```bash
python git_push_server.py
```

### 访问上传页面
- **本机访问**: http://localhost:8080
- **网络访问**: http://192.168.2.9:8080

### 上传并自动推送
1. 选择HTML课件文件
2. 点击"🚀 开始上传并推送到Git"
3. 系统自动完成：
   - 保存文件到uploads目录
   - 添加文件到Git
   - 创建提交记录
   - 推送到远程仓库

## 📋 功能特性

### 智能文件管理
- **唯一文件名**: 防止冲突
- **时间戳记录**: 追踪上传时间
- **Git版本控制**: 完整的提交历史

### 实时状态显示
- ✅ Git仓库状态监控
- 📊 上传进度显示
- 🔗 GitHub链接展示

### 安全特性
- 📁 本地备份存储
- 🌐 安全的网络传输
- 🔒 Git版本回滚能力

## 🌐 GitHub访问方式

### 查看上传的文件
在GitHub仓库中查看：
```
https://github.com/您的用户名/初中数学课件库/tree/main/uploads
```

### 分享给其他人
生成的GitHub链接可以直接分享：
- 🌐 网页访问：直接打开HTML文件
- 📱 移动设备：支持响应式浏览
- 💻 教学使用：投屏展示

## 📊 实际应用场景

### 1. **教师个人课件库**
```bash
# 创建一个专门存放自己课件的仓库
git remote add origin https://github.com/您的用户名/我的课件库.git
```

### 2. **教研组共享库**
```bash
# 创建一个教研组共享的仓库
git remote add origin https://github.com/教研组/数学课件集合.git
```

### 3. **学生作业提交**
```bash
# 学生可以fork仓库并提交作业
git remote add upstream https://github.com/老师/数学课件库.git
```

## 🔄 版本控制示例

### 自动生成的提交信息
```
添加课件文件: 1767003056_819289_八年级上册第二章无理数在数轴上表示（黄权锋）.html (2025-12-29 17:30:15)
```

### Git历史记录
```bash
# 查看提交历史
git log --oneline

# 查看特定文件的修改历史
git log uploads/文件名.html
```

## ⚠️ 注意事项

### 网络要求
- 需要稳定的网络连接进行Git推送
- 推送失败时文件仍会保存到本地

### 仓库管理
- 定期同步远程仓库
- 合理设置.gitignore避免推送无关文件

### 安全提醒
- 不要推送敏感信息
- 公开仓库注意版权问题

## 🎯 优势对比

| 功能 | 原始版本 | Git推送版本 |
|------|----------|-------------|
| 本地存储 | ✅ | ✅ |
| 跨设备访问 | ✅ | ✅ |
| 版本控制 | ❌ | ✅ |
| 在线分享 | ❌ | ✅ |
| 协作编辑 | ❌ | ✅ |
| 备份同步 | ❌ | ✅ |

## 📞 技术支持

如果遇到Git推送问题：
1. 检查网络连接
2. 确认Git认证配置
3. 验证远程仓库地址
4. 查看详细错误日志

---
*Git自动推送功能 - 让课件管理更智能*